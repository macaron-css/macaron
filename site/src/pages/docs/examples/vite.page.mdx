import { DocsLayout as Layout } from '../docs-layout';
import { Stackblitz } from './stackblitz';

export const documentProps = {
  title: 'Vite example — macaron',
};

<Layout>
# Vite Example

<Stackblitz example="vite" />

</Layout>
